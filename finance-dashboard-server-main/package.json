{
  "name": "finance-dashboard-backend",
  "version": "1.0.0",
  "description": "Assessment backend for a finance dashboard system.",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env src/server.js",
    "dev": "node --env-file=.env --watch src/server.js",
    "test": "node --env-file=.env --test --test-concurrency=1 --test-isolation=none"
  },
  "dependencies": {
    "pg": "^8.20.0"
  }
}
